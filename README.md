# deej-rs

## Development setup

Instructions for setting up the development environment.

### Pre-commit hooks

Install the [pre-commit](https://pre-commit.com/) package manager.

Run `pre-commit install` to install the pre-commit hooks. These will run before every commit.